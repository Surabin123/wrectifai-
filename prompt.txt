<USER_REQUEST>
WRECTIFAI — CRITICAL PRODUCTION FIX. DO NOT GIVE ME A PLAN. DO NOT GIVE ME A SUMMARY. INSPECT THE EXISTING CODEBASE, FIND THE ROOT CAUSES, IMPLEMENT THE FIXES, TEST THEM, AND ONLY THEN REPORT WHAT WAS ACTUALLY CHANGED.

I HAVE BEEN WORKING ON THIS PROJECT FOR 20 DAYS. I HAVE NOW WASTED 7 HOURS TODAY BECAUSE THE SAME TASKS KEEP BEING REPEATED, EXISTING WORKING FUNCTIONALITY KEEPS GETTING BROKEN, AND BASIC REQUIREMENTS KEEP BEING IGNORED. I WILL NOT ACCEPT ANOTHER PARTIAL OR SUPERFICIAL IMPLEMENTATION. DO NOT MAKE ME REPEAT THESE REQUIREMENTS AGAIN.

THIS IS AN EXISTING PRODUCTION-INTENDED APPLICATION. DO NOT REBUILD IT FROM SCRATCH. DO NOT CREATE A DEMO. DO NOT MOCK DATA. DO NOT HARD-CODE BUSINESS DATA. DO NOT PATCH THE SCREEN ONLY. TRACE EACH PROBLEM THROUGH DATABASE → BACKEND → API → API CLIENT → FRONTEND AND FIX THE ACTUAL ROOT CAUSE.

============================================================
PHASE 1 — MANDATORY CODEBASE INSPECTION BEFORE EDITING
============================================================

Before modifying code, inspect the existing implementation and understand:

- frontend architecture
- Next.js app structure
- existing components
- existing API client
- backend/API routes
- database schema/models
- authentication
- authorization/RBAC
- garage registration
- admin garage management
- customer garage listing
- customer garage details
- location handling
- currency handling
- service catalog
- services offered
- bookings
- quotes
- service requests
- wallet/payments
- existing garage records

Specifically inspect the code behind:

/dashboard
/garages
/garages?garage=...
/admin/garages
/admin/service-requests
/admin/bookings
/admin/quotes

Also inspect src/lib/api-client.ts and every API route used by these pages.

DO NOT start changing random components before understanding the data flow.

Create an internal dependency/data-flow map while inspecting:

Database
→ API
→ API client
→ state
→ component
→ displayed value

For every incorrect value below, find where the value becomes incorrect.

============================================================
PHASE 2 — ABSOLUTE RULES
============================================================

DO NOT:

- rewrite the application
- replace the existing architecture
- create duplicate APIs
- create duplicate database tables
- create fake business data
- hard-code ratings
- hard-code garage locations
- hard-code prices
- hard-code currency symbols
- hard-code customer counts
- hard-code garage highlights
- hard-code warranty information
- hard-code services
- use random fallback values
- reset the database
- delete legitimate existing data
- replace working authentication
- break RBAC
- modify unrelated modules
- hide errors instead of fixing them
- use `||` fallbacks to disguise missing database fields
- make the UI look correct while leaving the API/data layer broken

Reuse existing architecture wherever possible.

If a database field is missing, make the smallest proper schema/model change required.

If an API contract is wrong, fix the API contract.

If the frontend mapping is wrong, fix the frontend mapping.

If existing data is invalid, correct the data carefully without destroying legitimate records.

============================================================
PHASE 3 — FIX THE FINANCIAL DATA MAPPING FIRST
============================================================

There is a serious data-mapping bug in the admin details screens.

The screenshots show values such as:

Customer phone:
9483724707

but the Total Amount becomes something like:

12345543210

or:

null 33

or:

94837247071,890

THIS IS A DATA MAPPING BUG, NOT A UI BUG.

Trace:

database
→ backend
→ API response
→ api-client.ts
→ React state
→ modal

Identify exactly why phone/email/amount/price fields are being mixed.

A phone number MUST NEVER become a total amount.

A phone number MUST NEVER be concatenated with a price.

`null` MUST NEVER appear inside an amount.

Fix the actual field mapping and API response types.

Correctly distinguish:

- customer name
- customer phone
- customer email
- garage
- vehicle
- issue description
- service
- quote amount
- booking amount
- total amount
- payment status

Every amount must originate from the correct database/API field.

If a legitimate amount does not exist, display:

"Price on request"

or another appropriate empty state.

Do NOT display:

null
undefined
null 33
0.00

unless zero is genuinely the actual stored price.

============================================================
PHASE 4 — CENTRALIZED DYNAMIC CURRENCY SYSTEM
============================================================

The application currently mixes:

USD
$
₹
undefined

This must be completely fixed.

Currency must NOT be hard-coded independently inside individual components.

Create/use one authoritative currency resolution system.

The architecture must effectively be:

USER / SELECTED LOCATION
→ COUNTRY
→ CURRENCY CODE
→ CENTRAL CURRENCY FORMATTER
→ ALL PRICE COMPONENTS

Use proper currency codes internally:

INR
USD
AED
etc.

For India/Bengaluru:

₹549
₹1,299
₹2,999

For USA/New York:

$499
$1,299

For UAE/Dubai:

AED ...

Do NOT force USD on every user.

Do NOT assume every user is Indian.

Do NOT infer exact physical location from a phone number alone. Use the existing user/location architecture and selected location appropriately.

The same currency resolution must be used by:

- dashboard
- garage cards
- garage details
- services
- service catalog
- bookings
- quotes
- offers
- wallet/payments
- any other price display

There must be ONE reusable currency formatter.

No screen should independently decide whether to show `$`, `₹`, `USD`, etc.

============================================================
PHASE 5 — DYNAMIC LOCATION SYSTEM
============================================================

The dashboard location selector currently behaves incorrectly.

When searching:

Bengaluru

it says:

"No cities found"

Fix this properly.

When the user searches/selects:

Bengaluru

Bengaluru must appear and become the active location.

Also support:

Mumbai
Kochi
New York
Dubai

and make the system extensible for future locations.

The location system must support:

country
state/province
city
locality/area
currency
currency code

The selected location must be shared across the customer application and affect:

- garage listing
- garage details
- locality display
- available garages
- currency
- pricing

Do NOT create separate hard-coded city logic inside individual pages.

============================================================
PHASE 6 — GARAGE LOCATION DATA
============================================================

Garage cards currently show:

undefined

for location.

This is unacceptable.

Existing garages already have locality/area information, including the existing records where locality such as Koramangala has been provided.

Trace:

Garage Registration
→ Database
→ Backend
→ API
→ API client
→ Garage card
→ Garage details

Find where the location is being lost.

Fix the real mapping.

Do NOT do:

location || "Bengaluru"

Do NOT do:

city || "Bengaluru"

Do NOT invent locations.

If the database genuinely has no locality, show an appropriate empty state such as:

"Location not provided"

instead of JavaScript `undefined`.

============================================================
PHASE 7 — GARAGE REGISTRATION MUST BE THE SOURCE OF CUSTOMER-FACING DATA
============================================================

This is critical.

The customer garage page currently displays information that the garage registration form never asked the garage owner to provide.

Examples include:

- reviews
- ratings
- happy customer counts
- garage highlights
- years in service
- warranty
- service benefits
- response time
- service catalog information
- other garage-specific information

This is WRONG.

The application must never magically invent business information.

Inspect:

Admin
→ Garage Management
→ Register Garage

Compare every registration field against every field displayed in:

- garage cards
- garage detail page
- garage highlights
- services offered
- warranty/benefits
- operating information
- service catalog
- pricing

If a customer-facing value is supposed to be configurable, the registration flow must collect it.

If it is supposed to be derived, it must be derived from real database data.

There must be no hard-coded fake values.

============================================================
PHASE 8 — ADMIN GARAGE REGISTRATION
============================================================

Audit the existing garage registration form and database model.

Ensure the registration process can legitimately collect the information required by the existing product.

Where applicable, support:

GARAGE INFORMATION:
- garage name
- owner name
- phone
- email
- description
- garage image
- status
- verification

LOCATION:
- country
- state
- city
- locality/area
- address
- postal code
- coordinates if already supported/required

OPERATING INFORMATION:
- working hours
- response time
- years in service

SERVICES:
- general service
- engine repair
- AC service
- brake & suspension
- battery service
- tyres & wheel care
- diagnostics
- additional services

BENEFITS:
- warranty availability
- warranty duration
- free inspection
- free pickup
- pick & drop
- original/genuine parts
- pay after service
- other actual supported benefits

HIGHLIGHTS:
Only display garage highlights that are actually entered or legitimately derived.

Do NOT automatically create:

"4000+ Happy Customers"

Do NOT automatically create:

"20+ years in service"

Do NOT automatically create:

"Certified Technicians"

unless the database actually contains the information.

============================================================
PHASE 9 — REVIEWS AND RATINGS
============================================================

Newly registered garages must NOT receive fake ratings.

Do NOT hard-code:

4.5
4.7
4.3
etc.

If there are no reviews:

show:

"No reviews yet"

or:

"0 Reviews"

with no fake rating.

Ratings and review counts must come from the actual review system/database.

Do not invent review counts.

Do not seed fake reviews just to make the UI look populated.

============================================================
PHASE 10 — GARAGE SERVICES
============================================================

The current garage detail page shows service categories such as:

General Service
Engine Repair
AC Service
Brakes & Suspension
Battery Service
Tyres & Wheel Care
Diagnostics
More Services

These must be data-driven.

Do NOT assume every garage provides every service.

If a garage registered:

General Service
AC Service
Battery Service

show those.

If another garage registered:

Engine Repair
Diagnostics
Brake Repair

show those.

The service list must come from the actual garage/service relationship in the database.

============================================================
PHASE 11 — MORE SERVICES UI
============================================================

The current Service Catalog page is too large and visually poor.

DO NOT show the entire large service catalog page when the customer clicks "More Services".

Instead:

Click "More Services"
→ open a compact modal/pop-up.

The modal should display additional services using compact icon-based items.

Example structure:

More Services

[icon] Oil & Filter Change
[icon] Periodic Maintenance
[icon] Wheel Alignment
[icon] Battery Replacement
[icon] AC Service
[icon] Transmission Service
[icon] Brake Repair
[icon] Electrical Repair

Use the project's existing icon library where possible.

Do NOT create giant cards.

Do NOT create a huge vertically stacked catalog.

Do NOT create unnecessary nested dropdowns.

This must work for ALL garages, not just AutoCare Plus.

============================================================
PHASE 12 — SERVICE CATALOG PRICES
============================================================

The current service catalog displays:

USD 0.00

for many services.

This is unacceptable.

Prices must come from actual service/garage pricing data.

If a service has a real configured price:

format it dynamically according to the selected location currency.

If no price has been configured:

display:

"Price on request"

Do NOT display fake:

USD 0.00

Do NOT invent service prices.

============================================================
PHASE 13 — WARRANTY AND GARAGE BENEFITS
============================================================

The UI currently shows things such as:

Warranty Available
1 Month Warranty
Free Inspection
Free Pickup
Original Parts
Genuine Parts
Pay After Service
Quick Service

These must come from actual garage configuration.

If a garage has not selected/configured a benefit, do not display it.

The admin registration/configuration flow must be capable of storing these values.

============================================================
PHASE 14 — GARAGE DETAIL PAGE UI
============================================================

The AutoCare Plus detail page currently has inconsistent sizing compared with the existing application.

Do NOT redesign the entire application.

Make the garage detail page consistent with the existing WrectifAI design system.

All garages must use the same reusable layout:

- hero image dimensions
- typography
- spacing
- service cards
- booking panel
- sidebar
- buttons
- responsive behavior

The data changes between garages, not the fundamental layout.

============================================================
PHASE 15 — GARAGE DETAIL PERFORMANCE
============================================================

Clicking a garage currently takes too long to load.

Do not simply add a loading spinner.

Investigate the actual performance issue.

Check for:

- duplicate API requests
- sequential API requests that can be combined
- unnecessary database queries
- repeated location requests
- repeated service requests
- unnecessary client-side fetching
- large image loading
- redundant rendering

Fix the underlying problem.

The garage detail page must load efficiently in production.

============================================================
PHASE 16 — ADMIN GARAGE MANAGEMENT
============================================================

The Admin Garage Management page currently shows:

Total Garages
Active Garages
Inactive Garages

These must be calculated from the actual database.

Do NOT hard-code:

14
12
2
13

etc.

The calculations must reflect actual records.

Example:

Total = all valid garages
Active = status ACTIVE
Inactive = status INACTIVE

The counters must update after:

- registering garage
- activating garage
- suspending garage
- removing/deactivating garage where permitted

============================================================
PHASE 17 — REMOVE THE EYE BUTTON
============================================================

The current Admin Garage table has:

eye button
+
three-dot button

Remove the eye button.

Keep ONLY:

⋮

When clicking the three dots:

For an ACTIVE garage:

View Details
Suspend

For an INACTIVE/SUSPENDED garage:

View Details
Make Active

Nothing else.

============================================================
PHASE 18 — DROPDOWN POSITIONING
============================================================

There is a specific UI bug with the last garage in the table.

When clicking the three-dot menu on the last garage, the dropdown opens downward and goes outside the visible screen.

Fix the dropdown positioning.

The menu must automatically determine whether there is enough space below.

If there is not enough space:

open upward.

Example:

normal row:

⋮
↓
View Details
Suspend

bottom row:

View Details
Suspend
↑
⋮

Do not allow the menu to be clipped by the viewport/table/container.

This must work regardless of which row is clicked.

============================================================
PHASE 19 — ADMIN GARAGE ACTIVATION/SUSPENSION
============================================================

Currently:

Garage
→ three dots
→ Make Active
→ Confirm

results in:

Not Found

or:

Invalid action

This is a real backend/API problem.

Do NOT just catch the error in the frontend.

Trace:

button
→ action handler
→ api-client.ts
→ API endpoint
→ route
→ validation
→ authorization
→ database update

Find why the endpoint/action is returning:

404 Not Found

or:

Invalid action

Fix the backend/API contract.

The action must actually update the database.

After confirmation:

1. API succeeds
2. database status changes
3. modal closes
4. garage list refreshes
5. status badge updates
6. active/inactive counter updates

Do the same for:

ACTIVE → SUSPEND

and:

INACTIVE/SUSPENDED → MAKE ACTIVE

============================================================
PHASE 20 — CUSTOMER GARAGE LIST
============================================================

The customer garage page currently displays cards with:

- undefined location
- hard-coded-looking ratings
- inconsistent currency
- garage information not entered during registration

Fix the data source.

The garage card must display only legitimate data.

For example:

Garage Name
Rating only if reviews exist
Review count only if reviews exist
Actual locality
Actual distance if implemented
Actual response time if configured
Actual benefits
Actual pricing
Actual availability

Do not invent missing values.

============================================================
PHASE 21 — GARAGE COUNTS / EXISTING DATA
============================================================

There are existing garage records, including records previously identified as unwanted:

Metro Auto Bay
demo garage 1

Do not blindly delete records from the database.

Inspect how these records are currently being loaded and whether they are legitimate production records, demo/seed records, inactive records, or stale records.

If they were specifically created as demo/stale data and the existing project requirement is that they must not appear, remove/deactivate them through the correct data-management mechanism.

Do NOT delete unrelated legitimate garages.

The customer-facing list and admin totals must reflect actual intended records.

============================================================
PHASE 22 — NO `undefined` ANYWHERE
============================================================

Search the relevant application for places where raw values can render:

undefined
null
NaN

Especially inspect:

- city
- locality
- country
- currency
- price
- amount
- rating
- review count
- garage description
- service name
- service price
- response time
- years in service
- highlights

Do not merely hide these with random fallbacks.

Fix the underlying source/mapping.

For genuinely optional data, use a meaningful empty state such as:

Not provided
No reviews yet
Price on request

Never expose JavaScript values to the customer.

============================================================
PHASE 23 — LOCATION-SPECIFIC GARAGE DATA
============================================================

When Bengaluru is selected, the system should be capable of showing Bengaluru garages with actual locality data.

For example, existing garage records may have areas such as:

Koramangala
Indiranagar
Whitefield
Jayanagar
etc.

When Mumbai is selected, show actual Mumbai locations.

When Kochi is selected, show actual Kochi locations.

When New York is selected, show actual New York locations.

When Dubai is selected, show actual Dubai locations.

DO NOT hard-code random garage names/areas into the React page.

The architecture must support adding more garages through Admin Registration.

============================================================
PHASE 24 — USER LOCATION + CURRENCY MUST NOT BE MIXED
============================================================

A user's phone number is NOT itself an exact location.

Use appropriate signals from the existing application:

- selected location
- stored user location
- country
- profile data
- available location service if already implemented

Use the selected location as the authoritative UI context once the user chooses a location.

Example:

User opens application
→ detected/stored location is India
→ currency INR

User selects Bengaluru
→ active city Bengaluru
→ INR

User selects New York
→ active city New York
→ USD

User selects Dubai
→ active city Dubai
→ AED

The location selector must therefore control the location context and currency context consistently.

============================================================
PHASE 25 — API CONTRACT VALIDATION
============================================================

For every affected API endpoint verify:

REQUEST
→ VALIDATION
→ AUTHORIZATION
→ DATABASE QUERY
→ RESPONSE
→ FRONTEND TYPE
→ UI

Make sure frontend field names exactly match backend response field names.

Do not solve mismatches with:

any
unknown
random casts
property guessing
fallback chains

Use proper TypeScript interfaces/types.

Avoid unnecessary `any`.

============================================================
PHASE 26 — PRODUCTION REQUIREMENTS
============================================================

The application must work in production on:

Vercel
Render

Do not rely on localhost-specific behavior.

Check:

- environment variables
- API base URLs
- server/client boundaries
- CORS
- authentication cookies
- database connectivity
- production API routes
- error handling

There must be no:

404 API errors
500 errors
API fail to fetch
CORS errors
hydration errors
syntax errors
TypeScript errors
broken buttons
broken actions
missing routes

============================================================
PHASE 27 — SECURITY
============================================================

Do not weaken authentication or authorization.

Do not trust frontend-provided:

- garage ownership
- role
- user ID
- amount
- payment status
- permissions
- garage status

Backend must validate authorization.

Do not expose sensitive database information.

Do not modify existing RBAC unless a current bug directly requires it.

============================================================
PHASE 28 — TEST THE COMPLETE FLOWS
============================================================

Do not stop after compiling.

Test the actual flows.

TEST 1 — LOCATION

Login
→ dashboard
→ open location
→ search Bengaluru
→ Bengaluru appears
→ select Bengaluru
→ active location changes
→ garages update
→ currency becomes INR

Then test:

Mumbai
Kochi
New York
Dubai

Verify both location and currency.

TEST 2 — GARAGE REGISTRATION

Admin
→ Garage Management
→ Register Garage
→ enter garage details
→ enter location
→ select services
→ configure benefits
→ configure warranty
→ save

Then verify:

Admin list
→ garage appears

Customer:

Garages
→ garage appears

Garage details
→ exactly the registered information appears

No magical information appears.

TEST 3 — REVIEWS

New garage
→ no reviews

Customer should NOT see fake:

4.5
4.7
4.3

Instead:

No reviews yet

until actual reviews exist.

TEST 4 — SERVICES

Garage details
→ Services Offered
→ More Services
→ modal opens
→ service icons appear
→ services match that garage's actual configured services

TEST 5 — CURRENCY

India
→ ₹

USA
→ $

UAE
→ AED

No USD hard-coded across India screens.

TEST 6 — ADMIN ACTION

Inactive garage
→ ⋮
→ Make Active
→ Confirm
→ database updated
→ status becomes ACTIVE
→ counter updates

Active garage
→ ⋮
→ Suspend
→ Confirm
→ database updated
→ status becomes INACTIVE/SUSPENDED
→ counter updates

TEST 7 — BOTTOM DROPDOWN

Click three-dot menu on the final visible row.

Verify menu opens upward and all options remain visible.

TEST 8 — FINANCIAL DETAILS

Open:

Service Request
Booking
Quote

Verify:

phone ≠ amount
email ≠ amount
amount is numeric
currency is correct
no null/undefined concatenation

============================================================
PHASE 29 — BUILD / TYPECHECK / LINT
============================================================

After implementation:

- run the project's existing build
- run TypeScript checks
- run lint if configured
- resolve errors caused by your changes
- inspect browser console
- inspect network/API failures
- inspect server logs
- test affected routes

Do NOT disable TypeScript errors just to make the build pass.

Do NOT disable lint rules just to hide problems.

Do NOT add `@ts-ignore` unless there is an absolutely legitimate existing reason.

============================================================
PHASE 30 — FINAL VERIFICATION
============================================================

Before telling me the work is complete, verify that these are all true:

[ ] No phone number is displayed as an amount
[ ] No `null 33`
[ ] No `undefined` displayed to users
[ ] No fake prices
[ ] No fake ratings
[ ] No fake review counts
[ ] No fake happy-customer numbers
[ ] No fake garage highlights
[ ] No hard-coded USD
[ ] INR works for India
[ ] USD works for USA
[ ] AED works for UAE
[ ] Bengaluru search works
[ ] Mumbai search works
[ ] Kochi search works
[ ] New York search works
[ ] Dubai search works
[ ] Garage locality comes from actual data
[ ] Garage registration collects required customer-facing data
[ ] Services are garage-specific
[ ] More Services opens a compact modal
[ ] Service catalog is not unnecessarily huge
[ ] Service prices come from actual data
[ ] Missing prices show a proper empty state
[ ] Reviews are database-backed
[ ] Admin garage count is database-backed
[ ] Activate works
[ ] Suspend works
[ ] No 404 Not Found during activation
[ ] No Invalid action error
[ ] Eye button removed
[ ] Three-dot menu contains View Details + correct status action
[ ] Bottom-row dropdown opens upward
[ ] Garage details load without unnecessary delay
[ ] No API fail-to-fetch
[ ] No CORS errors
[ ] No TypeScript errors
[ ] No syntax errors
[ ] No hydration errors
[ ] Existing authentication still works
[ ] Existing RBAC still works
[ ] Existing unrelated functionality remains intact
[ ] Production configuration remains compatible with Vercel and Render

============================================================
FINAL RULE
============================================================

DO NOT TELL ME "IMPLEMENTATION COMPLETE" JUST BECAUSE THE PAGE LOOKS BETTER.

The implementation is complete ONLY when the underlying data flow works:

DATABASE
→ BACKEND
→ API
→ API CLIENT
→ FRONTEND
→ UI

and the entire flow has been verified.

If you discover that an existing implementation is fundamentally wrong, fix the root cause rather than adding another workaround on top of it.

If you discover missing database fields, make the correct minimal migration.

If you discover incorrect API contracts, correct them.

If you discover incorrect frontend mappings, correct them.

If you discover fake/hard-coded data, remove it and replace it with real database-driven behavior.

DO NOT MAKE ME REPEAT THIS WORK AGAIN.

I have already spent 20 days on this project and 7 hours today repeating the same tasks. I need a stable, production-ready implementation now, not another temporary patch that breaks something else tomorrow.

START BY INSPECTING THE EXISTING CODEBASE. THEN FIX THE ROOT CAUSES. THEN TEST THE COMPLETE FLOWS. ONLY AFTER THAT REPORT THE FILES CHANGED, ROOT CAUSES FOUND, FIXES IMPLEMENTED, AND TEST RESULTS.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T19:15:57+05:30.

The user's current state is as follows:
Other open documents:
- d:\WRECTIFIAI\wrectifai\apps\web\src\app\admin\users\generate_customers.js (LANGUAGE_JAVASCRIPT)
- d:\WRECTIFIAI\wrectifai\apps\api\src\config\database.ts (LANGUAGE_TYPESCRIPT)
- d:\WRECTIFIAI\wrectifai\apps\web\add_mappin_import.js (LANGUAGE_JAVASCRIPT)
- d:\WRECTIFIAI\wrectifai\apps\web\replace_hrefs.js (LANGUAGE_JAVASCRIPT)
- d:\WRECTIFIAI\wrectifai\apps\api\src\modules\admin\admin.routes.ts (LANGUAGE_TYPESCRIPT)
No browser pages are currently open.
Running terminal commands:
- pnpm run api (in d:\WRECTIFIAI\wrectifai, running for 12m40s)
- pnpm run web (in d:\WRECTIFIAI\wrectifai, running for 12m34s)

The user has uploaded 1 audio file(s). Consider them as a part of their message.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.1 Pro (Low). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>